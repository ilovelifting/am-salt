// Placeholder until a real domain is chosen — update this before deploying,
// it feeds metadataBase (OG/Twitter image URLs), robots.txt, and sitemap.xml.
export const siteUrl = "https://amsalt.example.com";
